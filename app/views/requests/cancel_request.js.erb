$('.flash-messages').empty().append('<%= flash_messages(@request) %>');

<% if @request.errors.empty? %>
	window.ws.send(JSON.stringify({ event: 'notification_create', receiver_id: '<%= @receiver_id %>' }));
	window.location.href = '<%= requests_path %>';
<% end %>
