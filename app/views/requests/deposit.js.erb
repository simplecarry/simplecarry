$('.flash-messages').empty().append('<%= flash_messages(@request) %>');

<% if @request.errors.empty? %>
	window.ws.send(JSON.stringify({ event: 'notification_create', receiver_id: '<%= @request.selected_offer.carrier_id %>' }));
	window.location.href = '<%= request_path(params[:id]) %>';
<% end %>
