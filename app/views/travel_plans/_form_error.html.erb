<% if @travel_plan.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@travel_plan.errors.count, "error") %>.
    </div>
    <ul>
    <% @travel_plan.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
