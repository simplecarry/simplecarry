$('.flash-messages').append('<%= flash_messages(@request) %>');

<% if @comment.errors.empty? %>
  var content = "<%= j render @comment %>";
  window.ws.send(JSON.stringify({ content: content, event: 'comment_create' }));
<% end %>
